Support Task 276959

From  Nelina Kareeva:

1. Базата е BPB_TA_REGRESSION_NEXT на TESTSERVER18\SQL2016Back Up правим всеки ден тук : \\TESTSERVER18\Daily_BackUps_TA\BPB_TA_REGRESSION_NEXT

    1.1 Това са ни пилотните кейсове, за които сме настроили всички критерии:
        select * from PREV_COMMON_TA
        where ta_type like '%BETA%'
 

    1.2. В колона TEST_CASE в таблица PREV_COMMON_TA да опитам да опиша с думи това което искаш за всеки ред (т.е. очакването ни на каква комбинация и критерии отговаря накратко) ?


    Additional Description:
        $/VSS CSoft/Projects Documentation/CSoft/Active/TestAutomation/TestAutomation 2022/TestScenarios/TestScenario_CashPayment.xlsx 


    Описание на ситуацията:
        Досега, при автоматичен тест се създават поредност от клиенти, сделки и т.н. 
        Трябва да добавим нови случаи, при които няма да е необходимо създаването им, като вместо да ги създаваме ще търсим съществуващи по набор от критерии.

        Трябва да се разработят инструменти за търсене на сделки, клиенти, пълномощници и всичко необходимо за изпълнение на конкретен тест кейс.

    Резултат от анализ:


2. Изискване:
    2.1. Да се добави нова SP за търсене на клиенти и сделки за автоматичен тест.
        Търсенето ще е по критерии, описани в различни таблици в TA базата (база с входни данни за автоматичен тест).
        Да се помисли за "олекотен" вариант за търсене при вече изпълнено първоначално търсене - например да се "пре-проверяват" само избрани критерии , които са най - динамични като салдо, неплатени такси и други , които се промянат при тестане.
 
    2.2. Номерата на намерените клиенти и сделки трябва да се запишат в определени таблици в ТА базата.

3. Списък на критериите за търсене, алгоритъма за търсене,  данните за update и параметризацията на процедурата е описано в отделен документ:
    $/VSS CSoft/Projects Documentation/CSoft/Active/TestAutomation/TestAutomation 2022/TestScenarios/TestScenario_CashPayment.xlsx – шийт „ClientAndDealSearchCriteria“.
 
Да се организира допълнителна среща за уточняване на всички изисквания с работещия по задачата.

Допълнителна информация:
Н.К. Да се тества от екип Лондон.
